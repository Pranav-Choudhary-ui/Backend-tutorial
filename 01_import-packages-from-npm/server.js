const cat = require('cat-me')

console.log(cat())


// node module contain all the files of the package
// package.json contains the list of packages my code is using
// package-lock.json contains the list of all the dependencies -> our code + packages imported

// to start a node.js app professionally
// in terminal -> npm init -y